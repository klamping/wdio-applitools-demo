# WebdriverIO Applitools Demo

After setting your API key, run the following commands to try these tests out.

```shell
git clone https://github.com/klamping/wdio-applitools-demo.git
cd wdio-applitools-demo
npm install
npx wdio
```
